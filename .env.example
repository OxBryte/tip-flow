# Frontend Environment Variables

# Backend URL
NEXT_PUBLIC_BACKEND_URL=http://localhost:3001

# Backend Wallet Address (fetched dynamically from backend)
# NEXT_PUBLIC_BACKEND_WALLET_ADDRESS is no longer needed - fetched from /api/backend-wallet

# Other configuration
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=your_walletconnect_project_id