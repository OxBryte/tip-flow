{
  "name": "ecion-batch-contracts",
  "version": "1.0.0",
  "description": "Ecion batch transfer contracts",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network base",
    "deploy-batch-tip": "hardhat run scripts/deployBatchTip.js --network base"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0"
  },
  "dependencies": {}
}
